class Constants
{
    static GameWidth  = 320;
    static GameHeight = 480;
    
    static CenterX = Math.floor(Constants.GameWidth / 2);
    static CenterY = Math.floor(Constants.GameHeight / 2);
}

export default Constants;